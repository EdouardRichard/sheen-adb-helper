# 第三方依赖与许可证

> v0.01，初始清单 2026-07-14，结项复核 2026-07-15。版本以 `gradle/libs.versions.toml` 与 Gradle 解析结果为准。直接依赖仍在白名单内，但 Kadb 的运行时传递依赖 `spake2-java 1.0.5` 为 GPL-3.0-or-later，不符合当前仓库白名单，发布合规暂缓。

| 依赖 | 版本 | 许可证 | 用途 | 体积/替代方案 |
|---|---:|---|---|---|
| Android Gradle Plugin | 9.1.1 | Apache-2.0 | Android 构建与 R8 | 仅构建时；无运行时体积 |
| Kotlin / Compose compiler | 2.3.20 | Apache-2.0 | Kotlin 与 Compose 编译 | 技术基线，无等价小型替代 |
| Compose BOM / UI / Material 3 | 2026.06.01 | Apache-2.0 | 简体中文、自适应 UI | 官方按需组件；未引入第三方 UI 框架 |
| Activity Compose | 1.13.0 | Apache-2.0 | Activity、SAF Activity Result | 官方最小集成 |
| AndroidX Core | 1.18.0 | Apache-2.0 | v0.05 直接提供 `androidx.core.content.FileProvider`，仅用于日志单文件临时只读分享 | 官方维护；移除 v0.05 分享能力后删除直接声明 |
| AndroidX Lifecycle | 2.10.0 | Apache-2.0 | ViewModel、生命周期安全 StateFlow 收集 | 官方基线；避免自建生命周期层 |
| AndroidX DataStore Preferences | 1.2.0 | Apache-2.0 | 设备档案非敏感元数据 | 小型持久化层；替代为 SQLite 会更重 |
| Kotlin Coroutines | 1.10.2 | Apache-2.0 | 取消、超时、Flow、IO 调度 | 核心异步基线 |
| Kadb | 2.1.1 | Apache-2.0 | ADB、TLS、配对码、Shell 流 | AAR 约 226 KiB，密码学传递依赖较大；适配器可替换为 AOSP/自有实现 |
| Okio | 3.17.0 | Apache-2.0 | Kadb 流与旧式 ADB 合并输出的有界读取 | Kadb 已传递引入，本项目显式固定同版本以适配流；替换随 Kadb 适配器进行 |
| Bouncy Castle bcprov/bcpkix | 1.83 | MIT | 证书与密码学 | 主要体积来源之一；由 R8 裁剪，替换需协议复验 |
| spake2-java | 1.0.5 | **GPL-3.0-or-later（不在白名单）** | Android 11+ SPAKE2 配对 | Kadb 运行时传递依赖；阻断发布合规，处置见 ADR 0004 |
| ed25519/curve25519-elisabeth | 0.1.0 | MIT | SPAKE2 曲线运算 | 二级传递依赖 |
| AndroidX DocumentFile | 1.1.0 | Apache-2.0 | Kadb Android artifact 传递能力 | 本应用不调用文件管理；若 Kadb 拆包可移除 |
| HiddenApiBypass | 6.1 | Apache-2.0 | Kadb Android TLS 兼容传递能力 | 本应用不直接调用；商店/Android 16 待真机复核 |
| TestNG | 7.11.0 | Apache-2.0 | JVM 单元测试 | 仅测试，不进入 APK |

## v0.04 接入前依赖许可证与门禁（T001，2026-07-22）

下表保留接入前的静态供应链判断；实际接入状态见其后的 T073 复核。发布 POM 是精确坐标和依赖图的权威来源；许可证以精确版本/标签的仓库 LICENSE 为准（`apk-parser` POM 另有 BSD-2-Clause 声明）。ZXing core 3.5.4 POM 没有 `<licenses>`，因此其 Apache-2.0 仅由 `zxing-3.5.4` 标签 LICENSE 证明；仓库归档状态用于维护风险判断。

| 坐标 | 精确版本 / 许可证 | 受限用途 | 传递依赖处理 | 维护、替代与移除 | 当前门禁状态 |
|---|---|---|---|---|---|
| `com.google.zxing:core` | `3.5.4` / Apache-2.0（白名单内） | 仅由 `:feature:devices` 的项目自有 `QrMatrixEncoder` 编码 QR；没有 scanner、camera integration 或 JavaSE artifact。 | POM 仅有 `junit:junit` 的 test scope；运行时无传递依赖，禁止把该测试项作为运行时依赖加入。 | 上游未归档，3.5.4 于 2025-11-11 发布；中等维护风险（无路线图承诺）。替代为自有 QR 编码器；移除时删除 ZXing 适配器、catalog alias 和 `:feature:devices` implementation，并重跑互操作测试。 | **条件通过，可接入 T002/T003。** Gradle 图、Debug 编译与 QR 互操作尚未实际验证，不能声称已通过 Android runtime。 |
| `net.dongliu:apk-parser` | `2.6.10` / BSD-2-Clause（白名单内） | 仅在 `:core:adb` 内部、可替换的 APK 元数据适配器解析 label、资源和图标；不得向公开模型泄漏第三方类型。 | POM 中 `jsr305:3.0.0`、`bcprov-jdk15on:1.58`、`bcpkix-jdk15on:1.58` 均为 `optional=true`，`junit:4.12` 为 test scope。Gradle 不传递加入 optional 项；默认不显式添加。若未来需要任一 optional 项，必须先分别完成许可证白名单、固定版本和解析图审查。 | 上游已归档，**高维护风险**。替代为自有受限 Manifest/resources parser，或返回 Plan 选择重新审查的已维护实现；移除时删除内部适配器、catalog alias 和 `:core:adb` implementation，保留自有模型/有界 reader。 | **高风险、仅条件接入；US4 为 BLOCKED。** 只有实际通过恶意 ZIP 防护（traversal、压缩炸弹、损坏 ZIP、资源边界）及 Android API 30 runtime smoke test 后才能解除。任一失败即保持 US4 `BLOCKED` 并返回 Plan 选替代 parser；不得用整项 `UNSUPPORTED` 或仅包名占位宣称应用名/图标需求完成。 |

### T001 当时的证据状态与发布边界

- T001 当时 Android API 30 runtime、恶意 APK/ZIP、Gradle 解析和体积测量均未执行；后续实际状态由下方 T073 复核取代。
- 新依赖门禁、功能完成和设备验证均不解除既有发布阻断：在 ADR 0004 规定的条件完成前，`spake2-java 1.0.5` 的 GPL-3.0-or-later 仍使发布合规为 **BLOCKED**。

### T001 一手来源

- [ZXing core 3.5.4 published POM](https://repo.maven.apache.org/maven2/com/google/zxing/core/3.5.4/core-3.5.4.pom)；[ZXing 3.5.4 release](https://github.com/zxing/zxing/releases/tag/zxing-3.5.4)；[ZXing LICENSE](https://github.com/zxing/zxing/blob/zxing-3.5.4/LICENSE)
- [apk-parser 2.6.10 published POM](https://repo.maven.apache.org/maven2/net/dongliu/apk-parser/2.6.10/apk-parser-2.6.10.pom)；[archived upstream repository](https://github.com/hsiafan/apk-parser)；[apk-parser LICENSE](https://github.com/hsiafan/apk-parser/blob/master/LICENSE)

## v0.04 当前解析与许可证状态（T073，2026-07-23）

| 依赖 | 当前运行时图 | 制品大小 | LICENSE / NOTICE 状态 | 门禁 |
|---|---|---:|---|---|
| ZXing core 3.5.4 | 由 `:feature:devices` 直接接入，运行时无传递依赖 | 610,364 bytes（缓存 JAR） | JAR 与当前 Debug APK 均无 ZXing LICENSE/NOTICE entry；仓库文档指向精确标签的 Apache-2.0 LICENSE | 自动化/Debug 构建通过；Android 11–16 QR 互操作 `NOT_RUN`；发布归档/应用内许可证待补 |
| apk-parser 2.6.10 | 由 `:core:adb` 直接接入；POM optional 的 `jsr305`、旧 Bouncy Castle 均不在实际运行时图 | 213,647 bytes（缓存 JAR） | JAR 与当前 Debug APK 均无 apk-parser LICENSE/NOTICE entry；仓库文档指向 BSD-2-Clause LICENSE | 恶意/损坏输入自动化通过；Android API 30 runtime `NOT_RUN`；发布归档/应用内许可证待补 |
| spake2-java 1.0.5 | 仍由 Kadb 2.1.1 传递进入运行时图 | 11,001 bytes（缓存 JAR） | JAR 无 LICENSE/NOTICE entry；精确版本 LICENSE 为 GPL-3.0-or-later | 不在白名单，ADR 0004 未关闭，发布 **BLOCKED** |

当前 `app-debug.apk` 为 42,742,434 bytes；缺少同配置接入前基线，不能计算 ZXing/apk-parser 净增量，Release/R8 体积仍为 `NOT_RUN`。应用内许可证页面尚未列出 ZXing/apk-parser，并错误标注 spake2-java；在修正展示、随分发提供所需许可证文本及关闭 ADR 0004 前，构建成功不构成发布许可。

维护与移除路径不变：ZXing 中风险，可用项目自有 QR encoder 替换；apk-parser 高风险，可用项目自有受限 Manifest/resources parser 替换。二者均通过项目自有 adapter 隔离，移除时同步删除对应 Gradle alias/implementation 并重跑各自契约测试。

未引入 DI、导航、图片加载、网络业务、分析、广告、崩溃上报、支付或 JUnit EPL 依赖。Compose 仪器化测试所常用的 JUnit 4 依赖不在许可证白名单，因此 v0.01 使用 TestNG 覆盖 UI 状态/菜单策略与纯逻辑，真机清单覆盖实际 Compose 语义与交互。

依赖源码与许可证来源详见原项目发布页；Kadb 的详细选型和回滚策略见 ADR 0001。

## 结项复核说明

- `spake2-java 1.0.5` 的发布 POM 与[精确标签 LICENSE](https://github.com/Flyfish233/spake2-java/blob/1.0.5/LICENSE) 均指向 GPL-3.0-or-later；原 Apache-2.0 记录已更正。
- 当前应用内许可证页面仍沿用原记录，本轮遵守“只做结项文档、不修改业务代码”的边界，未修改该页面。许可证冲突解决后，应用内展示、NOTICE 和本文必须一并复核。
- 处置方案与完成门禁见 [`adr/0004-spake2-license-compliance.md`](adr/0004-spake2-license-compliance.md)。
